# 简化认证系统测试环境配置
# 复制这些内容到 .env 文件中

# 环境模式
ENV_MODE=local

# JWT配置
JWT_SECRET_KEY=your-super-secret-jwt-key-for-testing-only

# 数据库配置（PostgreSQL）
DATABASE_URL=postgresql://username:password@localhost:5432/your_database

# Redis配置（可选，用于测试）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_SSL=false

# 可选配置（设为None避免验证错误）
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DAYTONA_API_KEY=
DAYTONA_SERVER_URL=
DAYTONA_TARGET=
TAVILY_API_KEY=
RAPID_API_KEY=
FIRECRAWL_API_KEY= 